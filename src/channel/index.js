const BaseAdapter = require('./base-adapter');
const WebAdapter = require('./web-adapter');
const WeComAdapter = require('./wecom-adapter');
const ChannelRegistry = require('./registry');

module.exports = { BaseAdapter, WebAdapter, WeComAdapter, ChannelRegistry };
